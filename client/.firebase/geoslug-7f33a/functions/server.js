import { onRequest } from 'firebase-functions/v2/https';
  const server = import('firebase-frameworks');
  export const ssrgeoslug7f33a = onRequest({"region":"us-west1","maxInstances":1}, (req, res) => server.then(it => it.handle(req, res)));
  